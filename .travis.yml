language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-purescript' ]; then cd .. && eval "mv $currentfolder generator-purescript" && cd generator-purescript; fi

